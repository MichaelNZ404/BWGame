using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/**
    Handles building status as well as influence ring creation
**/
public class temple_controller : MonoBehaviour
{
    public int playerNumber;
    public bool isBuilt = true;
    public int health = 100;

    // Start is called before the first frame update
    void Start()
    {
        // TODO, handle influence of all city centers owned by the player
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
